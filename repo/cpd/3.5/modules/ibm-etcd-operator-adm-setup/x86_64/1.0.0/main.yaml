# type is mandatory here
type: module
category: adm-setup
# The name here just for display purpose, not necessary to be the same as the name in the directory. Optional
name:
# The description is optional
description: ""
# Mandatory - List all the files needed to generate the exception, cpd will download/execute those yamls listed here
files:
- adm/crd/ibm-etcd-operator-crd.yaml
- adm/crd/ibm-etcd-operator-backup-crd.yaml
- adm/crd/ibm-etcd-operator-restore-crd.yaml
- adm/roles/ibm-etcd-operator-role.yaml
- adm/rb/ibm-etcd-operator-rb.yaml
- adm/sa/ibm-etcd-operator-sa.yaml 
