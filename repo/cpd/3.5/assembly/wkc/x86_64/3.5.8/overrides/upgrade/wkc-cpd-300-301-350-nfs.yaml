##################################################################################################
## Overrides specifically for an upgrade from CP4D 3.0.0 to CP4D 3.0.1 to CP4D 3.5.0/3.5.3 for WKC
##################################################################################################

solr-chart:
  solr:
    volumeClaim:
      accessMode: ReadWriteMany

iis-chart:
  xmetarepoVolumeInfo:
    volumeClaim:
      accessMode: ReadWriteMany
  serviceVolumeInfo:
    volumeClaim:
      accessMode: ReadWriteMany
