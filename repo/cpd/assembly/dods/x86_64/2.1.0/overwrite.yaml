# Overwrite for the installation of WSL (i.e. replicates WSL overwrite.yaml)
# this is needed as if we install WSL through DODS dependency, its overwrite.yaml is
# not taken into account
global:
  ibmProduct: zen
  replicaCount: 1
  ha: false
